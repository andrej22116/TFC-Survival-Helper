export const RECEIPT_CASTING = 'casting';
export const RECEIPT_WELD = 'weld';
export const RECEIPT_PLAN = 'plan';

const RECEIPT = {
    CASTING: RECEIPT_CASTING,
    WELD: RECEIPT_WELD,
    PLAN: RECEIPT_PLAN,
};

export default RECEIPT;